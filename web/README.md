RusteETL UI (Vite + React + React Flow)

Quick start

1. cd web
2. npm install
3. npm run dev

This will start a Vite dev server on http://localhost:5173 by default.

The UI contains a minimal React Flow canvas with a single node. It's a starting
point for building the ETL pipeline visual editor.
