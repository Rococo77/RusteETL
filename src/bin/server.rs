fn main() {
    // Placeholder server binary. The real async server is gated behind a feature
    // and may require specific runtime setup. This stub keeps cargo builds
    // and CI from failing while preserving the file for reference.
    println!("server binary placeholder - enable the 'server' feature to run the real server");
}
